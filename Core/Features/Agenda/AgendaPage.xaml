<?xml version="1.0" encoding="utf-8" ?>
<local:TabbedItemPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Xamarin.Summit"
             x:Class="Xamarin.Summit.AgendaPage">
    <ContentPage.Content>

        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <local:TabHeaderItem Grid.Row="0" Grid.Column="0" Header="{Binding HeaderOne}" Current="{Binding CurrentHeader}" Command="{Binding HeaderClickCommand}" />
            <local:TabHeaderItem Grid.Row="0" Grid.Column="1" Header="{Binding HeaderTwo}" Current="{Binding CurrentHeader}" Command="{Binding HeaderClickCommand}" />


            <Label Grid.ColumnSpan="2" Grid.Row="1" Text="{Binding Message}"
                IsVisible="{Binding ShowMessage}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

        </Grid>
    </ContentPage.Content>
</local:TabbedItemPage>